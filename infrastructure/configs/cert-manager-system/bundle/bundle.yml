apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: thecluster-lan-ca
spec:
  sources:
    - secret:
        name: "thecluster.lan"
        key: tls.crt
  target:
    configMap:
      key: ca.crt
