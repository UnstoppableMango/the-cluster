apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    operator: thecluster
    operator-deployment: kubernetes-operator
  name: thecluster-operator
  namespace: thecluster-system
spec:
  replicas: 1
  revisionHistoryLimit: 0
  selector:
    matchLabels:
      operator: thecluster
      operator-deployment: kubernetes-operator
  template:
    metadata:
      labels:
        operator: thecluster
        operator-deployment: kubernetes-operator
    spec:
      containers:
      - env:
        - name: POD_NAMESPACE
          valueFrom:
            fieldRef:
              fieldPath: metadata.namespace
        image: accessible-docker-image:latest
        name: operator
        resources:
          limits:
            cpu: 100m
            memory: 128Mi
          requests:
            cpu: 100m
            memory: 64Mi
      terminationGracePeriodSeconds: 10
