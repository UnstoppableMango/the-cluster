apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: origin-ca-issuer
  namespace: cloudflare-system
spec:
  sourceRef:
    kind: GitRepository
    name: origin-ca-issuer
  path: ./deploy/crds
  interval: 10m
  prune: false
  wait: true
