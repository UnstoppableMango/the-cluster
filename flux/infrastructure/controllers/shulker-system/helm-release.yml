apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: shulker
  namespace: shulker-system
spec:
  interval: 10m
  releaseName: shulker
  targetNamespace: shulker-system
  chart:
    spec:
      chart: shulker-operator
      version: 0.13.0
      sourceRef:
        kind: HelmRepository
        name: shulker
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
  values: {}
