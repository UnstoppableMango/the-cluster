apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: awx-operator-crds
  namespace: awx-system
spec:
  sourceRef:
    kind: GitRepository
    name: awx-operator
  path: ./config/crd
  interval: 30m
  prune: false
  wait: true
