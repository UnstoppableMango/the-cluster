apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: awx-operator
  namespace: awx-system
spec:
  sourceRef:
    kind: GitRepository
    name: awx-operator
  path: ./config/default
  interval: 30m
  prune: true
  targetNamespace: awx-system
  wait: true
