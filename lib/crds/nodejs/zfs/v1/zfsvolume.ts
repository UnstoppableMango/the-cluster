// *** WARNING: this file was generated by crd2pulumi. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../../types/input";
import * as outputs from "../../types/output";
import * as utilities from "../../utilities";

import {ObjectMeta} from "../../meta/v1";

/**
 * ZFSVolume represents a ZFS based volume
 */
export class ZFSVolume extends pulumi.CustomResource {
    /**
     * Get an existing ZFSVolume resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, opts?: pulumi.CustomResourceOptions): ZFSVolume {
        return new ZFSVolume(name, undefined as any, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'kubernetes:zfs.openebs.io/v1:ZFSVolume';

    /**
     * Returns true if the given object is an instance of ZFSVolume.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ZFSVolume {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ZFSVolume.__pulumiType;
    }

    public readonly apiVersion!: pulumi.Output<"zfs.openebs.io/v1" | undefined>;
    public readonly kind!: pulumi.Output<"ZFSVolume" | undefined>;
    public readonly metadata!: pulumi.Output<ObjectMeta | undefined>;
    /**
     * VolumeInfo defines ZFS volume parameters for all modes in which ZFS volumes can be created like - ZFS volume with filesystem, ZFS Volume exposed as zfs or ZFS volume exposed as raw block device. Some of the parameters can be only set during creation time (as specified in the details of the parameter), and a few are editable. In case of Cloned volumes, the parameters are assigned the same values as the source volume.
     */
    public readonly spec!: pulumi.Output<outputs.zfs.v1.ZFSVolumeSpec>;
    /**
     * VolStatus string that specifies the current state of the volume provisioning request.
     */
    public readonly status!: pulumi.Output<outputs.zfs.v1.ZFSVolumeStatus | undefined>;

    /**
     * Create a ZFSVolume resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: ZFSVolumeArgs, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (!opts.id) {
            resourceInputs["apiVersion"] = "zfs.openebs.io/v1";
            resourceInputs["kind"] = "ZFSVolume";
            resourceInputs["metadata"] = args ? args.metadata : undefined;
            resourceInputs["spec"] = args ? args.spec : undefined;
            resourceInputs["status"] = args ? args.status : undefined;
        } else {
            resourceInputs["apiVersion"] = undefined /*out*/;
            resourceInputs["kind"] = undefined /*out*/;
            resourceInputs["metadata"] = undefined /*out*/;
            resourceInputs["spec"] = undefined /*out*/;
            resourceInputs["status"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ZFSVolume.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * The set of arguments for constructing a ZFSVolume resource.
 */
export interface ZFSVolumeArgs {
    apiVersion?: pulumi.Input<"zfs.openebs.io/v1">;
    kind?: pulumi.Input<"ZFSVolume">;
    metadata?: pulumi.Input<ObjectMeta>;
    /**
     * VolumeInfo defines ZFS volume parameters for all modes in which ZFS volumes can be created like - ZFS volume with filesystem, ZFS Volume exposed as zfs or ZFS volume exposed as raw block device. Some of the parameters can be only set during creation time (as specified in the details of the parameter), and a few are editable. In case of Cloned volumes, the parameters are assigned the same values as the source volume.
     */
    spec?: pulumi.Input<inputs.zfs.v1.ZFSVolumeSpecArgs>;
    /**
     * VolStatus string that specifies the current state of the volume provisioning request.
     */
    status?: pulumi.Input<inputs.zfs.v1.ZFSVolumeStatusArgs>;
}
