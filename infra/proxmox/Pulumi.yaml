name: thecluster-proxmox
runtime: nodejs
description: Proxmox infrastructure for THECLUSTER
config:
  nodes:
    value:
      - zeus
      - apollo
  versions:
    value:
      talos: 1.6.5 # renovate depName=siderolabs/talos extractVersion=^v(?<version>.*)
