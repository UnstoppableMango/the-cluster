syntax = "proto3";

option csharp_namespace = "ServiceConnector.Protos";

message Index {
  string name = 1;
  string apiKey = 2;
  string torznabFeed = 3;
}

message IndexReply {
  string message = 1;
}

service Indexer {
  rpc Publish (Index) returns (IndexReply);
}
