version: '3'
services:
  talos-controlplane:
    networks:
      default:
        ipv4_address: '10.5.0.2'

networks:
  default:
    labels:
      talos.cluster.name: rosequartz
      talos.owned: false
    ipam:
      driver: default
      config:
        - subnet: 10.5.0.0/24
