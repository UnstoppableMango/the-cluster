name: thecluster-redis
runtime: nodejs
description: Redis Cluster install for THECLUSTER
config:
  versions:
    value:
      redis: 7.2.4-debian-11-r0 # renovate depName=bitnami/redis datasource=docker
