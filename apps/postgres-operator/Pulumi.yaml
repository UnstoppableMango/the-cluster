name: thecluster-postgres-operator
description: Zalando postgres-operator install for THECLUSTER
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
