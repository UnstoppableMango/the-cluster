name: thecluster-origin-ca-issuer
description: THECLUSTER install of origin-ca-issuer
runtime:
  name: nodejs
  options:
    packagemanager: yarn
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
  versions:
    value:
      # renovate: datasource=docker depName=cloudflare/origin-ca-issuer
      app: 0.12.1
      # renovate: datasource=helm depName=oci://ghcr.io/cloudflare/origin-ca-issuer-charts/origin-ca-issuer
      chart: 0.5.12
