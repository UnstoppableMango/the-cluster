name: thecluster-nginx-ingress
runtime: nodejs
description: Nginx Ingress install for THECLUSTER
config:
  versions:
    value:
      nginxIngress: 3.5.0 # renovate depName=nginx/nginx-ingress datasource=docker
