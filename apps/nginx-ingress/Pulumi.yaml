name: thecluster-nginx-ingress
runtime: nodejs
description: Nginx Ingress install for THECLUSTER
config:
  versions:
    value:
      nginxIngress: 3.4.3 # renovate depName=nginx/nginx-ingress datasource=docker
