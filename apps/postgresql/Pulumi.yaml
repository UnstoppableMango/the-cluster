name: thecluster-postgresql
runtime: nodejs
description: PostgreSQL install for THECLUSTER
config:
  versions:
    value:
      k8s: v1.28.0 # renovate depName=kubernetes/kubernetes
      bitnami:
        postgres: 16.1.0-debian-11-r15 # renovate depName=bitnami/postgresql datasource=docker
