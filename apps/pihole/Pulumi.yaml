name: thecluster-pihole
description: Pi-hole install for THECLUSTER
runtime:
  name: nodejs
  options:
    packagemanager: yarn
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
  versions:
    value:
      # renovate: datasource=docker depName=pihole/pihole
      docker: 2025.08.0
      chart: 2.35.0
      externalDns:
        # renovate: datasource=docker depName=registry.k8s.io/external-dns/external-dns
        docker: 0.19.0
        chart: 1.18.0
