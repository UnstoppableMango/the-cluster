apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db
  namespace: xmage
spec:
  storageClassName: fast-rwo
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
