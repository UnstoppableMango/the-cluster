name: thecluster-drone
runtime: nodejs
description: Drone CI install for THECLUSTER
config:
  versions:
    value:
      dind: 25-dind # depName=docker datasource=docker
      drone: v2.22.0 # depName=drone/drone datasource=docker
      droneGc: 1.0.0 # depName=drone/gc datasource=docker
      droneRunnerDocker: 1.8.3 # depName=drone/drone-runner-docker datasource=docker
