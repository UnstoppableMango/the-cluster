apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: plex-config
  namespace: media
  annotations:
    kustomize.toolkit.fluxcd.io/prune: disabled
spec:
  storageClassName: ssd-rbd
  accessModes:
    - ReadWriteOncePod
  resources:
    requests:
      storage: 500Gi
