name: thecluster-minio-operator
runtime: nodejs
description: Minio Operator install for THECLUSTER
config:
  versions:
    value:
      minioOperator: v5.0.12 # renovate depName=quay.io/minio/operator datasource=docker
