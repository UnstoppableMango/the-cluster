apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: necesse
  namespace: necesse
spec:
  storageClassName: fast-rwo
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
