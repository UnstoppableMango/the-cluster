name: thecluster-sealed-secrets
runtime: nodejs
description: Sealed Secrets install for THECLUSTER
config:
  versions:
    value:
      sealedSecrets: v0.24.4 # renovate depName=bitnami-labs/sealed-secrets
