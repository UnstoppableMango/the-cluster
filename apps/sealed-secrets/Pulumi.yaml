name: thecluster-sealed-secrets
runtime: nodejs
description: Sealed Secrets install for THECLUSTER
config:
  versions:
    value:
      sealedSecrets: v0.26.2 # renovate depName=bitnami-labs/sealed-secrets
