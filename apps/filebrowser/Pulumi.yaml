name: thecluster-filebrowser
runtime: nodejs
description: Filebrowser install for THECLUSTER
config:
  versions:
    value:
      filebrowser: v2.30.0 # renovate depName=filebrowser/filebrowser datasource=docker extractVersion=^(?<version>.*)-s6
