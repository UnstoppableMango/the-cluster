name: thecluster-capi-templates
runtime: nodejs
description: CAPI templates for THECLUSTER
config:
  versions:
    value:
      k8s: 1.29.1 # depName=kubernetes/kubernetes extractVersion=^v(?<version>.*)
      talos: 1.6.4 # depName=siderolabs/talos extractVersion=^v(?<version>.*)
      cloud-provider-proxmox: v0.0.4-rc1 # depName=sp-yduck/cloud-provider-proxmox
