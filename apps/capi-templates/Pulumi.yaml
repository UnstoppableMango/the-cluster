name: thecluster-capi-templates
runtime: nodejs
description: CAPI templates for THECLUSTER
config:
  versions:
    value:
      k8s: 1.29.1 # renovate depName=kubernetes/kubernetes extractVersion=^v(?<version>.*)
      talos: 1.6.4 # renovate depName=siderolabs/talos extractVersion=^v(?<version>.*)
      cloud-provider-proxmox: v0.0.4-rc1 # renovate depName=sp-yduck/cloud-provider-proxmox
