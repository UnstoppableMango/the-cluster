name: reconciler-test
runtime:
  name: nodejs
  options:
    packagemanager: yarn
description: A program for testing thecluster.Reconciler
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
